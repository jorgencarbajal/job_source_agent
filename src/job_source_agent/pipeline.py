"""Stage 1 and Stage 2, wired together behind the cache.

LinkedIn URL -> job id -> company identity -> listings URL. This is the single
entry point the API and the benchmark both call, so what Jobnova tests in the
browser is the same path the reported success rate measures.
"""
