"""Settings and secrets, read once from the environment.

Holds the ScrapingDog API key, the Anthropic model name, browser and request
timeouts, and the hop limit. Everything tunable lives here so the navigator and
the benchmark cannot drift apart on constants.
"""
